/**
 * This package contains classes to use with UI events.
 *<p>
 * Each new action class should extends the Action abstract class
 * that provide a base implementation common to every action
 *</p>
 * <p>
 * Each action is responsible to handle client-server communication
 * using {@link protocol} classes.
 * </p>
 */
package actions;